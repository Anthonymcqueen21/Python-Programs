#!/usr/bin/python

# Waiting for the user

raw_input("\n\nPress the enther key to exit.")
import sys; x = 'foo'; sys.stdout.write(x + '\n')

# Multiple Statement Groups as Suites

if expression:
   suite
elif expression:
   suite
else :
   suite
